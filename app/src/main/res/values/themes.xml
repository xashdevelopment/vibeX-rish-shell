<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme using Material Design Components -->
    <style name="Base.Theme.DefaultAppTheme" parent="@style/Theme.Material3.NoActionBar">
        <item name="android:colorPrimary">@color/md3_primary</item>
        <item name="android:colorAccent">@color/md3_primary</item>
        <item name="android:windowBackground">@color/md3_background</item>
        <!-- Required for colorControlNormal attribute used in drawables -->
        <item name="colorControlNormal">@color/md3_onSurface</item>
    </style>

    <style name="Theme.DefaultAppTheme" parent="Base.Theme.DefaultAppTheme" />
</resources>
